id = "interest_bug_fix"
name = "State POP interest bug fix"
version = "0.1"
module = "interest_bug_fix.dll"
conflicts = ["v2up"]

[settings]
version = 1

[[settings.fields]]
key = "debug"
type = "bool"
label = "Enable interest-fix diagnostics"
help = "Write per-state CSV diagnostics and emit interest telemetry. Disabled by default."
default = false
advanced = true
argv = { option = "-smedley-interest-fix-debug", codec = "bool_01" }
