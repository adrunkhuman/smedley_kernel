add_library(smedley_launcher_core STATIC src/launcher.cpp)
target_compile_definitions(smedley_launcher_core PRIVATE UNICODE _UNICODE WIN32_LEAN_AND_MEAN NOMINMAX)
target_include_directories(smedley_launcher_core PUBLIC "../../../include" PRIVATE "../../../third_party/toml/include")
target_link_libraries(smedley_launcher_core PRIVATE smedley_telemetry_registry bcrypt shell32 ole32)

if(BUILD_TESTING)
    add_executable(smedley_launcher_core_tests tests/launcher_test.cpp)
    target_compile_definitions(smedley_launcher_core_tests PRIVATE SMEDLEY_BUILD_DIR="${CMAKE_BINARY_DIR}" SMEDLEY_SOURCE_DIR="${CMAKE_SOURCE_DIR}")
    target_link_libraries(smedley_launcher_core_tests PRIVATE smedley_launcher_core GTest::gtest_main)
    add_dependencies(smedley_launcher_core_tests smedley_plugin_abi_test_plugin)
    add_dependencies(smedley_launcher_core_tests smedley_legacy_plugin_test_plugin)
    add_dependencies(smedley_launcher_core_tests interest_bug_fix)
    include(GoogleTest)
    gtest_discover_tests(smedley_launcher_core_tests DISCOVERY_TIMEOUT 30)
endif()
